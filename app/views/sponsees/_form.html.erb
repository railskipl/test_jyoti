<%= form_for(@sponsee) do |f| %>
  <% if @sponsee.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sponsee.errors.count, "error") %> prohibited this sponsee from being saved:</h2>

      <ul>
      <% @sponsee.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<% name = Circle.find(UserInvitation.find_by_email(current_user.email).circle_id).name %>
<% sponser =  Sponser.where("name like ?", name).first %>
<% sponser.sponsercfs.each do |s|%>
<%= s.custom_factor%>
<% end %>
  <div class="field">
    <%#= f.label :approve_admin_custom_factor  %><br /> 
    <%= f.label :approve_admin_custom_factor, "Reject", :value => "false"  %>
    <%= f.check_box :approve_admin_custom_factor %>
    <%= f.label :approve_admin_custom_factor, "Approve", :value => "false" %>
    <%= f.check_box :approve_admin_custom_factor %>
  </div>
<!-- 
   <div class="field">
    <%= f.label :custom_factor, "Custom Factor:" %>
   <%= f.collection_select :custom_factor, Sponsercf.all, :custom_factor, :custom_factor %>
   </div>   
 -->
<!-- 

  <div class="field">
    <%= f.label :your_choise_custom_factor %><br>
    <%= f.text_field :your_choise_custom_factor %>
  </div> -->
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
