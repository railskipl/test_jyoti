<h5>Individual</h5>

<%= form_for @individual,:html => { :multipart => true } do |f| %>
  <% if @individual.errors.any? %>
    <div id="error_explanation">
     <%= pluralize(@individual.errors.count, "error") %> prohibited this car model from being saved:

      <ul>
      <% @individual.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <%= f.label :title %>
    <%= f.text_field :title, :class=>"form-control",:style=>"width:300px;" %>


    <%= f.label :body %>

    <%= f.cktext_area :body %>


    <%= f.label :Uploadphoto %>

    <%= f.file_field :pic %>

   
  
   <div class="actions">
    <%= f.submit "Submit",:class=>"btn btn-primary"  %>
  </div>


<% end %>
