<%= link_to 'New Page', new_page_path,:class=>"btn btn-primary" %>
<br/><br/>
<div class="row">
<div class="col-xs-12">
<table class="table table-bordered">
 
 <tr>
    <th>Title</th>
     <th>Body</th>
     <th>Image</th>
      <th>Status</th>
      <th colspan=2>Actions</td>


  </tr>

<% @pages.each do |page| %>

  <tr>
    <td><%= page.title %></td>
    <td><%= page.body %></td>
    <td><%= image_tag page.photo.url, :width => "165", :height=>"100" %></td>
    <td style="text-align: center !important;"><%= link_to 'Edit', edit_page_path(page) %></td>
    <td style="text-align: center !important;"><%= link_to 'Destroy',page_path(page) ,:method => :delete , :data=>{ :confirm=>'Are you sure?' } %>
    </td>
  </tr>
<% end %>

</table>
</div>
</div>

