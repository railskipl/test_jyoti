
<%#= javascript_include_tag "jquery-1.5.2.min","jquery.validate.min","validation.js" %> 

<h2> Page</h2>
<div class="col-xs-4">
<%= form_for @page,:html => { :multipart => true,:id =>"new_page" } do |f| %>
  <% if @page.errors.any? %>
    <div id="error_explanation">
     <%= pluralize(@page.errors.count, "error") %> prohibited this car model from being saved:

      <ul>
      <% @page.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
	<p>
    <%= f.label :title %>
    <%= f.text_field :title, :class=>"form-control" %>
   </p>
 
  <p>
    <%= f.label :body %>

    <%= f.text_area :body,:class=>"form-control11" %>

  </p>

  <p>
    <%= f.file_field :photo %>
  </p>
   
  
   <div class="actions">
    <%= f.submit "Submit",:class=>"btn btn-primary"  %>
  </div>


<% end %>
</div>