<%= form_for(@factor) do |f| %>
  <% if @factor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@factor.errors.count, "error") %> prohibited this factor from being saved:</h2>

      <ul>
      <% @factor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :circle_id %><br>
    <%= f.collection_select :circle_id, Circle.all, :id, :name %>
  </div>
  <div class="actions">
    <%= f.submit "Save" ,:class => "btn btn-primary"%>
  </div>
<% end %>
